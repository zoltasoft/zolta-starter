# Experimental product layers

These layers are retained as product experiments but are not composed into the
production application. The standalone chat shell can be revisited for the
future AI interview-practice experience. Shared chat persistence used by the
document editor lives in the root Nuxt BFF instead.
