# Standalone application layers

These layers are deployable products that intentionally do not share the main
user application's route manifest. The admin application remains here with its
own layout, route middleware, API surface, and permission-aware navigation so it
can be hosted separately.
